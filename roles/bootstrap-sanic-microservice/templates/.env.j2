COMPOSE_PROJECT_NAME={{ repo.split('.')[0] }}
REGISTRY={{ registry }}
{{ repo.split('.')[0] | upper }}_SERVICE={{ repo.split('.')[0] }}_service
{{ repo.split('.')[0] | upper }}_SERVICE__VERSION=0.0.1.0
{{ repo.split('.')[0] | upper }}_SERVICE__CONTAINER={{ repo.split('.')[0] }}